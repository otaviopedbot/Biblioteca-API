<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit book</title>
</head>
<body>
  <h1>Edit book</h1>
  <form action="/books/<%= book[0].id %>?_method=PUT" method="POST">
    <label for="title">Title:</label>
    <input type="text" id="title" name="title" value="<%= book[0].title %>" required>
    <br>
    <label for="page">Pages:</label>
    <input type="number" id="page" name="page" value="<%= book[0].page %>" required>
    <br>
    <label for="quantity">Quantity:</label>
    <input type="number" id="quantity" name="quantity" value="<%= book[0].quantity %>" required>
    <br>
    <label for="author_id">Author ID:</label>
    <input type="number" id="author_id" name="author_id" value="<%= book[0].author_id %>" required>
    <br>
    <label for="bookshelve_id">Bookshelve ID:</label>
    <input type="number" id="bookshelve_id" name="bookshelve_id" value="<%= book[0].bookshelve_id %>" required>
    <br>
    <button type="submit">Update book</button>
  </form>
  <a href="/books">return</a>
</body>
</html>